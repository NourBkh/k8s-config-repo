apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azhelm.fullname" . }}-test-connection"
  labels:
    {{- include "azhelm.labels" . | nindent 6 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azhelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

